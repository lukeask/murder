# Tests

Philosophy and agent rules: [CONTRIBUTING.md](../CONTRIBUTING.md#testing-philosophy).

```bash
pytest              # one unit test today; grow slowly and deliberately
pytest tests/unit
```

`support/` and `fixtures/` are scaffolding for future cookbook/edge-case tests — not a license to pad coverage.
